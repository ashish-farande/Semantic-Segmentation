#TORUN: python main_train.py config_small.ini
# 128 goes out of memory
batch_size: 5
n_class: 10
lr: 0.001
epoch: 2
optimizer: 'Adam'
objective: 'CrossEntropyLoss'

crop :  True
horizontal_flip :  True
vertical_flip :  True
color_jitter :  True

model_name :  "best-model"

# model:{outdir}/best-model.pt
# curve:{outdir}/perf.csv
# iou per class:{outdir}/iou.csv
